/**
 * Images controller
 */
profileEditor.controller('ImagesEditor', function ($scope, profileService, util, messageService) {

    $scope.addImage = function () {
        alert("Not implemented yet. Would upload to biocache & store image in image service");
    };
});